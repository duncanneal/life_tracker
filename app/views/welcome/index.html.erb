<h1>Welcome to Life Tracker!</h1>
<h2>Tracking schools you've attended and life events experienced!</h2>

<h2>Recent Life Events</h2>
  
  <ul>
    <% @recent_events.each do |event| %>
      <li><%= event.title %>, <%= event.date %>, <%= event.description %></li>
    <% end %>
  </ul>

  <h2> Recent Schools</h2>

    <ul>
      <% @recent_schools.each do |school| %>
        <li><%= school.title %>, <%= school.start_year %> - <%= school.end_year %></li>
      <% end %>
    </ul>

<%= link_to 'People', people_path %>
<%= link_to 'Life Events', life_events_path %>
<%= link_to 'Schools', schools_path %>
