<p><id ="notice"> <%= notice %></p>
<nav class="top-bar" data-topbar role="navigation" data-options="is_hover: false" data-options="dropdown_autoclose: false">

  <ul class="title-area">
    <li class="name">
      <h1><%= link_to 'Life Tracker!', root_path %></h1>
    </li>
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="left">
      <li class="has-dropdown">
        <a href="#">Tracking</a>
        <ul class="dropdown">
          <li><a href="/life_events">People</a></li>
          <li><a href="/schools">Schools</a></li>
          <li><a href="/people">Life Events</a></li>
        </ul>
    </ul>
  </section>
</nav>
<h1>Listing Life Events</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Date</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @life_events.each do |life_event| %>
      <tr>
        <td><%= life_event.title %></td>
        <td><%= life_event.date %></td>
        <td><%= life_event.description %></td>
        <td><%= link_to 'Edit', edit_life_event_path(life_event) %></td>
        <td><%= link_to 'Destroy', life_event, method: :delete, data: { confirm: 'Are you sure?' }, class: "button alert small" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Welcome', root_path, class: "success button small" %>
<%= link_to 'New Life event', new_life_event_path, class: "button secondary small" %>

